{% set headingLevel = params.headingLevel if params.headingLevel else 3 %}
<div class="mod-do-dont{%- if params.classes %} {{ params.classes }}{% endif %}"{%- for attribute, value in params.attributes %} {{attribute}}="{{value}}"{% endfor %}>
  <h{{ headingLevel }} class="mod-do-dont__label">{{ params.title }}</h{{ headingLevel }}>
  <ul class="mod-do-dont__list {% if params.type == 'tick' %}mod-do-dont__list--tick{% else %}mod-do-dont__list--cross{% endif %}">
    {%- for data in params.items %}
    <li>
    {%- if params.type == 'cross' %}
      <span class="mod-do-dont__cross" aria-hidden="true"></span>
    {%- else %}
      <span class="mod-do-dont__tick" aria-hidden="true"></span>
    {%- endif %}{% if params.type == 'cross' %}{% if not params.hidePrefix %}Do not {% endif %}{% endif %}{{ data.item | safe}}
    </li>
    {%- endfor %}
  </ul>
</div>